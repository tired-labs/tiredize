[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tiredize"
version = "0.0.1"
description = "Schema driven markdown validation for TIRED Labs documentation."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["tiredize"]
